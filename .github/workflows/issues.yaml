name: issueComments

on:
  issues:
    types: [opened]
jobs:
  comment-with-action:
    runs-on: ubuntu-latest
    steps:
      - name: " showing context"
        run: echo `${{toJSON(github.event)}}` | jq .
